/**
 * 全局组件自动注册
 *
 * 在 main.ts 中导入即可自动注册所有共享组件：
 *   import '@/components/registerGlobal'
 *
 * 也可在页面中按需单独导入：
 *   import RightPanel from '@/components/layout/RightPanel.vue'
 */
import RightPanel from './layout/RightPanel.vue'

export { RightPanel }
